#include <iostream>
#include "cnf.h"

using std::cout;
using std::endl;
/*
int main()
{
  const char *fpath = "D:/docs/github_repo/CodingPractice/sat-20.cnf";
  Cnf cnf;
  cnf.parseFile(fpath);
  cnf.bmap->reset();
  for (uint32_t i = 3; i < 3 + 4; ++i) {
    cnf.markClauseExist(i, true);
  }
  cnf.show();
  return 0;
}
*/
